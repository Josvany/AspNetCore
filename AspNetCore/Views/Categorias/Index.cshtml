@model IEnumerable<AspNetCore.Models.Categoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.categoriaName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.catgoriaDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.categoriaStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.categoriaName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.catgoriaDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.categoriaStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.categoriaIntId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.categoriaIntId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.categoriaIntId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
